# entities

Business entities and how they're displayed (e.g. `user`, `product`). Each
entity slice typically has `ui` (display components), `model` (types/store),
and `api` (fetchers) segments. Entities may use `shared`, never `features`,
`widgets`, or `pages`.
