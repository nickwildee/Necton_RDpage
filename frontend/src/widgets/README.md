# widgets

Composite, self-contained UI blocks assembled from `features` and `entities`
(e.g. a page header, a sidebar, a comment section). A widget may be reused
across multiple pages but should not know which page renders it.
